import {StyleSheet} from 'react-native';

import {COLORS} from '@resources/colors';

export const headerStyle = StyleSheet.create({
  component: {
    padding: 3,
    backgroundColor: COLORS.BLUE.space_cadet,
  },
});
