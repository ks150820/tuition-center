import {StyleSheet} from 'react-native';

export const optionStyle = StyleSheet.create({
  outerComponent: {
    flex: 1,
  },
  commonComponent: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  checkboxComponent: {
    marginBottom: 0,
    padding: 10,
  },
  component: {
    marginTop: 5,
  },
});
