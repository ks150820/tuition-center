import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  component: {
    borderRadius: 1,
    overflow: 'hidden',
    elevation: 1,
  },
  imageStyle: {
    width: '100%',
    height: '100%',
  },
});
